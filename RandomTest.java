// Random Number Study
// GOAL: Generate random numbers and take data on those values.
//  Why?:  When a sensor input is calibrated with a mean, changes to that input will affect the system output.
//  Example:  A photo sensative resistor gets input and will change the direction of a robot.
//
//  Compile and run this Java code.  Leave it running when you leave the room and look at the results when you return.

class  RondomTest {
	public static void main (String a[]){
	  double x = 0;  // set x to zero
	double  min = 0; // minimum
	double max = 0; // maximum
	double mean = 0; //mean
	int ix = 0; // x as an integer  .  The doubles are chnged to integers to simulate sensor input
	int imin = 0;// integer minimum
	int imax = 0; // integer maximum
	int imean = 0; //integer mean
	long count = 0;
	for (;;)
		do(
		
			 {  // this is an infinte loop .  control+c to stop
			x = Math.random();	
			count++;
	}

	}
}

